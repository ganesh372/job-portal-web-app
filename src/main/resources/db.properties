# Database configuration (fallback when environment variables are not set)
# Override any of these by setting the corresponding environment variables:
#   DB_URL, DB_USERNAME, DB_PASSWORD
db.url=jdbc:mysql://localhost:3306/jobportal?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=root
